<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="example.web.infrastructure.datasource.todo.TodoMapper">
    <resultMap id="Todo" type="example.web.domain.model.todo.Todo">
        <constructor>
            <arg column="title" javaType="String"/>
            <arg column="description" javaType="String"/>
            <arg column="emergency" javaType="boolean"/>
        </constructor>
    </resultMap>

    <select id="findAll" resultMap="Todo">
        SELECT title
             , description
             , emergency
        FROM todo.todos
    </select>

    <select id="nextId" resultType="Long">
        SELECT nextval('todo.seq_todo_id') AS id
    </select>

    <select id="insert">
        INSERT INTO todo.todos(title, description, emergency)
        VALUES (#{title},
                #{description},
                #{emergency})
    </select>
</mapper>
